{
  "name": "@repo/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint --config .oxlintrc.json src --deny-warnings",
    "typecheck": "tsc -p ../../tsconfig.app.json && tsc -p ../../tsconfig.node.json"
  }
}
